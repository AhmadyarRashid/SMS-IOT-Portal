# Public env vars are bundled into the client at build time.
NEXT_PUBLIC_SMS_IOT_URL=https://go.smsiotpk.com
NEXT_PUBLIC_SMS_IOT_REALM=sms-iot

# Server-only — used by next.config.mjs rewrites to proxy /api, /auth, /websocket.
SMS_IOT_BACKEND_URL=https://go.smsiotpk.com
